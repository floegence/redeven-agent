# redeven-agent Development and Release Guide

> Scope: this repository (`redeven-agent/`).
>
> Goal: keep development, release, external delivery package mirror, and install-worker deployment consistent and auditable.

---

## 1. Git workflow (Worktree, required)

- Never develop directly on `main`.
- Every change must be done in a dedicated worktree + feature branch.
- `main` is only for `pull --ff-only` and merges.
- Resolve conflicts only inside the feature worktree, never on `main`.

Recommended template:

```bash
git fetch origin
git switch main
git pull --ff-only

BR=feat-<topic>
WT=../redeven-agent-feat-<topic>
git worktree add -b "$BR" "$WT" origin/main
```

Merge and cleanup:

```bash
git switch main
git pull --ff-only
git merge --no-ff "$BR"
git push origin main

git worktree remove "$WT"
git branch -d "$BR"
```

## 2. Release (full release)

### 2.1 Agent binary release (GitHub Release)

- Tag format: `vX.Y.Z`.
- Pushing the tag triggers `.github/workflows/release.yml`.
- Artifacts and signing files are published in GitHub Release.
- Release notes must include an operator-friendly preface (`scripts/generate_release_notes.sh`) plus GitHub auto-generated change notes.

### 2.2 external delivery package mirror + version endpoint (automatic)

- Workflow: `.github/workflows/sync-release-assets-to-r2.yml`
- Script: `scripts/sync_release_assets.sh`
- Trigger: automatic `workflow_run` (after `Release Agent` succeeds on `v*` tag), plus `release.published` fallback and manual `workflow_dispatch` re-sync

Workflow contract:

1. Download release assets from GitHub Release.
2. Verify checksum and Cosign signature.
3. Mirror files to package mirror under `release-assets/<tag>/...`.
4. Verify uploaded file hashes by re-downloading from R2.
5. Deploy the version-manifest Worker only after package mirror verification succeeds.

Manifest endpoint ownership:

- Worker URL: `https://version.agent.example.invalid/v1/manifest.json`
- Worker source: `deployment/private-delivery/workers/version-manifest/src/worker.mjs`
- Worker config: `deployment/private-delivery/workers/version-manifest/wrangler.toml`

Required repository secrets:

- `CLOUDFLARE_R2_ENDPOINT`
- `CLOUDFLARE_R2_ACCESS_KEY_ID`
- `CLOUDFLARE_R2_SECRET_ACCESS_KEY`
- `CLOUDFLARE_AGENT_PACKAGE_BUCKET`
- `CLOUDFLARE_API_TOKEN`
- `CLOUDFLARE_ACCOUNT_ID`

Failure policy:

- Mirror workflow fails fast.
- Existing manifest Worker deployment stays unchanged.
- GitHub Release remains immutable.

### 2.3 install.sh delivery

- `install.sh` source of truth: `scripts/install.sh`
- Download order in installer:
  - Primary: GitHub Release
  - Fallback: external delivery package mirror

### 2.4 Installer wrapper deployment (separate flow)

- external delivery Worker build/deploy is managed in downstream deployment automation.
- Production branch is fixed to `release`.
- Ensure `release` branch exists once:

```bash
git push origin origin/main:refs/heads/release
```

- Use this command only when installer/worker sources changed and need rollout:

```bash
./scripts/publish_delivery_branch.sh vX.Y.Z
```

## 3. Pre-commit checks (required)

- `sh -n scripts/install.sh`
- `sh -n scripts/publish_delivery_branch.sh`
- `sh -n scripts/sync_release_assets.sh`
- `sh -n scripts/generate_release_notes.sh`
- `./scripts/build_assets.sh`
- `go test ./...`
- `golangci-lint run ./...`
